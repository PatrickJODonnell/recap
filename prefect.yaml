# Prefect deployment configuration for CI/CD
deployments:
  - name: recap
    entrypoint: agent.py:process_users
    work_pool:
      name: Recap
    schedule:
      cron: "50 5 * * 0"
      timezone: "America/New_York"
